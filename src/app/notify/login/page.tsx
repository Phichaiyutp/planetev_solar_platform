import LineLogin from '@/components/notify/login/line_login'

const Home = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <LineLogin />
    </div>
    
  );
};

export default Home;
